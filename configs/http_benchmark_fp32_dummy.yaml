# HTTP benchmark configuration — dummy input

endpoint: http://127.0.0.1:8001/infer

request:
  input_type: dummy
  input: dummy
  top_k: 5

benchmark:
  warmup: 5
  repeat: 50
  timeout_sec: 10

output:
  json: outputs/reports/http_benchmark_mobilenetv3_small_onnx_fp32_dummy.json
  markdown: outputs/reports/http_benchmark_mobilenetv3_small_onnx_fp32_dummy.md
